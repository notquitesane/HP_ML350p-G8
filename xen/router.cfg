name = "router"
builder = "hvm"

memory = 512
vcpus = 1

disk = ['phy:/dev/vg/xen-router,xvda,w']

vif = ['bridge=br0,model=e1000']

boot = "c"

serial = "pty"
vnc = 1

on_reboot = "restart"
on_crash = "destroy"
